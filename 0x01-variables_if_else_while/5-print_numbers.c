#include <time.h>
#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Description: character desplay
 * Return: 0 means succes
 */
int main(void)
{
	int a = 0;

	while (a <= 9)
	{
		printf("%d", a);
		a++;
	}
		printf("\n");

	return (0);
}
